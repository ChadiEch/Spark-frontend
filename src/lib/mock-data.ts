// Mock data for Winnerforce Marketing Platform

import type { 
  User, Goal, Campaign, Ambassador, Post, Task, Asset, Activity, 
  KPICard, CalendarEvent, ProgressSnapshot 
} from '@/types';

// Empty mock data arrays
export const mockUsers: User[] = [];

export const mockGoals: Goal[] = [];

export const mockAmbassadors: Ambassador[] = [];

export const mockCampaigns: Campaign[] = [];

export const mockPosts: Post[] = [];

export const mockTasks: Task[] = [];

export const mockKPICards: KPICard[] = [];

export const mockCalendarEvents: CalendarEvent[] = [];

export const mockAssets: Asset[] = [];

// Export empty objects for compatibility
export const currentUser = null;
