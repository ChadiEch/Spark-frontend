# PRODUCTION ENVIRONMENT VARIABLES
# These are DEFAULT values for production builds.
# For Railway deployments, configure environment variables in the Railway dashboard.
# VITE_ variables are embedded at build time and cannot be changed at runtime.
# For dynamic configuration, use window.location detection in code.

NODE_ENV=production
VITE_APP_ENV=production
VITE_API_URL=https://spark-backend-production.up.railway.app/api
BACKEND_URL=https://spark-backend-production.up.railway.app